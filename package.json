{
  "name": "hello-gatsby",
  "version": "1.0.0",
  "private": true,
  "description": "hello-gatsby",
  "author": "Adam Finkelstein",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",

    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "deploy:ci": "gatsby build --prefix-paths && gh-pages -d public -r https://$GH_TOKEN@github.com/adamfinkelstein/hello-gatsby.git"
  },
  "dependencies": {
    "gatsby": "^5.11.0",
    "gh-pages": "^5.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
